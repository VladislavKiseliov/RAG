# RAGProgramm — TODO

## MVP: задачи до релиза

| # | Задача | Сервис | Приоритет | Статус |
|---|---|---|---|---|
| 1 | Один `.env` файл — убрать сервисные env, один корневой | все | Высокий | ✅ Готово |
| 2 | Логи — убрать `print()`, настроить structured JSON logging | все | Высокий | 🔄 Частично |
| 3 | Ошибки — кастомные исключения (ChatNotFoundError, LLMError, LLMUnavailableError), убрать HTTPException из сервисов | llm, backend | Высокий | ✅ Готово |
| 4 | Summary — ConversationService, count_after по БД, POST /llm/summary, generate_summary() | backend, llm | Высокий | ✅ Готово |
| 5 | Регистрация — валидация, роли (user/admin) | backend | Средний | 🔄 Частично |
| 6 | Профиль пользователя — расширить модель (имя, роль, аватар, дата) | backend, frontend | Средний | ⬜ Не начато |
| 7 | `GET /health` для llm_service | llm | Средний | ✅ Готово |
| 8 | Переименовать `history_massage` → `history_messages` | llm, backend | Низкий | ✅ Готово |
| 9 | Убрать мёртвый код — `agent_service.py`, `answer_service.py`, старый `context_builder.py`, `promt/promts.py` | llm | Низкий | ⬜ Не начато |
| 10 | Вынести параметры в settings — `top_k`, `chunk_size`, `max_context_chars` | rag, llm | Низкий | ⬜ Не начато |
| 11 | PDF склейка — `_fix_merged_prepositions` в `TextCleaner` | rag | Низкий | ⬜ Не начато |
| 12 | Тесты — прогнать все после изменений | все | Низкий | ⬜ Не начато |
| 13 | Конфиг — перейти на `config.toml` (URL, порты, таймауты, модели); секреты остаются в `.env` | все | Низкий | ⬜ Не начато |
| 14 | Merge `mvp` → `main` | — | — | ⬜ Не начато |

---

## Гибридный поиск — перевод RETRIEVER-слоя на пакетный гибридный поиск с фильтрацией

### Этап 1: Подготовка данных и локальная векторизация

- [ ] **Очистка текста перед индексацией**
  Добавить в пайплайн парсинга регулярные выражения (`re`) для автоматического разделения склеенных слов
  (`реконструируемыхиэксплуатируемых` → `реконструируемых и эксплуатируемых`).

- [ ] **Интегрировать локальный BM25-эмбеддер**
  Подключить класс `Bm25` из библиотеки `fastembed` (без передачи управления методам Qdrant).
  Настроить обучение (`fit()`) модели `qdrant/bm25` на всём корпусе документов при запуске сервера / индексации.

- [ ] **Обновить формат сохранения точек в Qdrant**
  Для каждого чанка генерировать плотный вектор (нейросеть) и разреженный (BM25, `indices` + `values`).
  Переписать `PointStruct` под именованные векторы: `dense_vector` и `bm25_sparse_vector`.

### Этап 2: Конфигурация Qdrant

- [ ] **Пересоздать коллекцию под гибридный поиск**
  В `vectors_config` — параметры плотного вектора (`size`, `distance=COSINE`).
  Добавить `sparse_vectors_config` для `bm25_sparse_vector`.

- [ ] **Полная переиндексация**
  Очистить старую коллекцию, залить данные с очищенным текстом и двойными векторами.

### Этап 3: Пакетный поиск (архитектура ретривера)

- [ ] **Переписать поиск на `query_batch_points`**
  Заменить одиночные вызовы на один пакетный запрос — принимает массив из 5+ запросов за раз.

- [ ] **Архитектура «Матрёшки» (Prefetch + Fusion)**
  Внутри каждого подзапроса поднять лимиты первичной выборки: `limit=40` для плотных и `limit=40` для разреженных векторов.
  Слияние результатов через `models.Fusion.RRF` на уровне Qdrant.
  Лимит сырых точек на выходе из Qdrant: `limit=30`.

### Этап 4: Постобработка и группировка

- [ ] **Обновить группировку `_group_and_sort_hits`**
  Эффективная группировка дочерних чанков по `parent_id` через словарь.
  Возвращать до **10** уникальных родительских объектов (вместо 5) для расширения контекста LLM.
  Заменить `.get("score")` на прямое обращение `["max_score"]` в цикле.

- [ ] **Score threshold после RRF**
  Скоры после RRF — маленькие дробные числа (~0.03). Экспериментально определить порог отсечения мусора и внедрить проверку перед добавлением родителя в финальный список.

### Этап 5: Интеграция и тестирование

- [ ] **Подключить обновлённый ретривер к нодам LangGraph**
  Убедиться, что стейт графа корректно принимает новый формат `list[list[RetrieveItem]]`.

- [ ] **Стресс-тест на «проблемных» запросах**
  Подать запрос с аббревиатурой (например, `ПРПСП`) — убедиться, что чанки со склеенными словами пессимизируются через RRF и не попадают в топ выдачи.