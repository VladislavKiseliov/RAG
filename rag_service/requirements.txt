# Легкие зависимости - используются rag-service (API) и flower.
# Тяжёлый Docling/torch/CUDA стек - только в requirements-worker.txt (rag-worker).

# Web
fastapi
uvicorn[standard]
pydantic-settings

# Database
sqlalchemy[asyncio]
asyncpg

# Task queue
celery[redis]
flower

# Storage
aioboto3

# Vector DB
qdrant-client

# Embeddings (sparse BM25 - лёгкий, нужен и API для поиска, и воркеру для индексации)
fastembed

# Текстовый сплиттер - маленький чистый Python-пакет (~300KB, без torch), но нужен и
# API: domain/chunking/chunk_builder.py в одном файле держит и ParentChunk (использует
# document_service.py, т.е. API), и ChildChunkBuilder (использует langchain-text-splitters) -
# разделять файл ради этого не стали, проще держать пакет в light
langchain-text-splitters

uuid6

# Морфология (A11: обратный поиск "полная форма -> аббревиатура" по леммам,
# без учёта падежа/числа) - чистый Python + словарь, без torch/GPU, годится
# для лёгкого API-образа
pymorphy3
pymorphy3-dicts-ru

# HTTP client
httpx

# Logging
python-json-logger
prometheus-client

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
alembic==1.18.4
psycopg2-binary==2.9.11
testcontainers==4.15.0
