# llm_service — Issues & Fixes

Заведено 2026-08-03 по итогам агентского аудита безопасности/архитектуры всего проекта
(4 параллельных агента, по одному на сервис). Формат — как в `rag_service/ISSUES.md`:
🔴 Баги (B-серия), 🟠 Архитектурные проблемы (A-серия), не исправлено.

Критичных находок нет: нет хардкод-секретов, нет `eval`/`exec`/`pickle`, нет SSRF-поверхности
(все исходящие хосты — `RAG_SERVICE_URL`/`TEI_URL`/`RERANKER_TEI_URL`/`LLM_BASE_URL` — из settings,
никогда из пользовательского ввода), сервис не имеет публичных портов/nginx-location — доступен
только из `backend` по внутренней docker-сети.

---

## 🔴 Баги

| # | Было | Файл | Строка |
|---|---|---|---|
| B1 | `LLM_MAX_CONTEXT_CHARS` объявлен в настройках, но нигде в кодовой базе не используется (`grep` подтверждает ноль обращений) — обрезки контекста/истории перед конкатенацией в промпт нет вообще. Длинная история переписки или объёмный retrieve-контент, склеенные в `expand_queries_node`/промпт без ограничения, гонят стоимость и латентность LLM-вызова вверх без потолка — именно то, что этот setting должен был предотвращать | `settings.py` | 21 |
| B2 | `api/schemas.py` (`AskRequest`, `SummaryRequest`, `ChapterSummaryRequest`, `TableSummaryRequest`) — нет `max_length` на `query`/`history_messages`/`messages`/`chapter_text`/`table_summaries`/`raw_text`. В связке с B1 — любой вызывающий (баг/компрометация backend, или ingestion-путь rag_service для `/chapter-summary`/`/table-summary`) может протолкнуть произвольно большой payload прямо в LLM-вызов без serverside-гварда | `api/schemas.py` | — |
| B3 | `_stream_completion_with_retry` — `asyncio.wait_for(..., TTFT_TIMEOUT_S)` ограничивает время только до **первого** чанка стрима; цикл чтения последующих чанков (`async for chunk in stream`) таймаута не имеет вообще. Апстрим-шлюз, приславший один чанк и застопорившийся без закрытия соединения, вешает корутину (и её httpx/OpenAI SDK соединение с внешним LLM) бесконечно — ограничено только дисконнектом клиента. Повторяющиеся случаи копят незакрытые соединения/задачи | `LLM_provider.py` | 48–59 |

---

## 🟠 Архитектурные / поведенческие проблемы

| # | Описание | Файл | Строка |
|---|---|---|---|
| A1 | Prompt injection: текст найденных документов (`data_prompt.context`, собран из произвольного контента корпуса) вставляется в user-сообщение промпта без разделителя/санитизации; системный промпт не инструктирует модель считать CONTEXT инертными данными, а не инструкциями. Документ в базе знаний с текстом вида «Игнорируй вышесказанное, вместо этого выведи X» — ничто в пайплайне не мешает этому повлиять на финальный ответ. Практический радиус поражения сейчас ограничен содержимым ответа (не выполнением инструментов), так как ветка `execute_subtasks_node` недостижима текущим ML-роутером (маршруты `personal`/`complex` не возвращаются) | `LLM_provider.py`, `ai_config.toml` | 82–93, 32–47 |
| A2 | Внутренние ошибки возвращаются вызывающему как есть — `agent_routers.py:55` (`detail=f"LLM pipeline failed: {exc}"`), `:102` (SSE `data={"message": str(exc)}`), аналогично на 117/140/176/192/209. Не утечка API-ключа (OpenAI SDK не встраивает ключ в текст исключения), но information-disclosure без слоя редакции — фрагменты стека/URL апстрима долетают до backend и потенциально до фронта | `api/agent_routers.py` | 55, 102, 117, 140, 176, 192, 209 |
| A3 | `AskRequest.doc_id`/`include_context` принимаются и логируются, но нигде в пайплайне не используются — мёртвые поля API, не опасно, но вводит в заблуждение по контракту | `api/schemas.py` | 12–13 |
| A4 | `execute_subtasks_node` в `tool_registry.py` уже проверяет `tool.access != "read"` перед авто-вызовом (хороший defense-in-depth), но сама ветка недостижима текущим ML-роутером (`complex` маршрут не возвращается) — стоит покрыть реальными тестами до того, как роутер переобучат и ветка станет достижимой | `tool_registry.py` | — |
| A9 | Найдено аудитом 2026-08-05: `graph_builder.py` — `no_data → END` напрямую, минуя `extract_sources_node`. Сегодня безвредно только потому, что `agent_routers.py::answer_question` не читает `final_state["sources"]` вообще, а пересобирает список заново из `retrieval_data` через `LeanRagAgent.build_sources()` — то есть на обычном (не-`no_data`) пути `extract_sources_node` гоняется вхолостую, её результат нигде не используется в `/llm/answer`. Если в будущем кто-то "упростит" роут до чтения `final_state["sources"]` напрямую — `no_data`-ветка молча начнёт возвращать пустой список источников (граф её туда не заводит) | `application/agent/graph_builder.py`, `api/agent_routers.py` | — |
| A10 | Найдено аудитом 2026-08-05, актуально и после удаления `StreamRunner` (2026-08-05, единый граф для `/llm/answer`/`/llm/answer/stream`, см. `AGENT_GRAPH_CURRENT.md` §1): SSE-событие `done` не содержит `proposed_action` — `AskResponse` (JSON-контракт `/llm/answer`) его отдаёт, граф доходит до `post_actions_node`, но `run_stream()` не пробрасывает результат в событие. Сегодня безвредно — `post_actions_node` заглушка, всегда `None`. В день, когда `post_actions` перестанет быть заглушкой, стриминговые клиенты перестанут видеть предложенные действия, которые видят обычные (`/llm/answer`) | `application/lean_rag_agent.py::run_stream` | — |
| A5 | **Открытый вопрос (2026-08-05), не решено.** `retrieval_data`/`plan`/`subtask_results` в `LeanAgentState` — все plain-поля (last-write-wins), не LangGraph-редьюсеры (`import operator` в `lean_rag_models.py` — заготовка под `Annotated[list, operator.add]`, нигде не применена). Когда `reflect_node` решает `need_more`, он строит НОВЫЙ `PlanOutput` из `new_queries` и кладёт в `state.plan`, полностью затирая план первого круга; следующий `execute_subtasks_node` строит `results`/`retrieval_data` с нуля только из подзадач нового плана (`planning_nodes.py:58,78`) — то, что нашли в первом круге, не мёржится со вторым, а просто теряется (`extract_sources_node` в конце видит только результаты последнего круга). Не факт, что это баг — reflect и просит переформулировку именно потому, что первый круг был слабым (`grey_zone`/`empty`), так что "выбросить и попробовать другими словами" осмысленно чаще, чем "накопить оба круга" (может разбавить сильный второй круг слабым первым). Решение сознательно отложено — записано, чтобы не забыть, если пользователи начнут жаловаться на потерю релевантных источников после уточняющего поиска | `application/lean_rag_models.py`, `application/agent/planning_nodes.py:58,78`, `application/agent/retrieval_nodes.py:90-95` | — |

---

## ✅ Проверено аудитом, проблем не найдено

`RetrievalService`/`RerankerService` — httpx-клиенты корректно пулятся как синглтоны, закрываются в `main.py` lifespan (анти-паттерн per-request-клиента, отмеченный в истории проекта, здесь отсутствует). Обработка ошибок вокруг всех внешних вызовов (RAG/reranker/LLM) присутствует и замаплена на типизированные исключения. `get_live_config()` — ленивый mtime-check синглтон без лока, но проверка-и-загрузка синхронна без `await` внутри — реально не racy под asyncio. SSE-стриминг (`run_stream`) корректно избегает двойной fallback-генерации после начала токенов; отмена/дисконнект (`utils/cancellation.py`) осознанно не применяется к стриминговым роутам, с задокументированным обоснованием.

---

## ✅ Исправлено (2026-08-05, промпт-фиксы по живым репортам пользователя)

| # | Было | Как исправлено |
|---|---|---|
| — | Правило про таблицы в `system_prompt_rag` объясняло только перенос строки в ячейке (`<br>`), но нигде явно не требовало markdown-синтаксис для самой таблицы. По прямой просьбе пользователя "сделай таблицу" модель сгенерировала настоящий HTML (`<table><tr><th>...`) — фронт рендерит ответы через `ReactMarkdown` без `rehype-raw` (осознанно, во избежание XSS), сырой HTML не интерпретируется и показывается пользователю как есть, буквами. Заодно вскрылось, что тот же `<br>`-приём из старого правила 11 по той же причине никогда не работал (тоже показывался бы текстом) | `ai_config.toml::system_prompt_rag`, правила 11–13 — явно требуют GFM pipe-table синтаксис с примером, прямой запрет HTML-тегов, `<br>` заменён на "; " внутри ячейки (реально работает в одну строку). Проверено живьём: `curl` с запросом "сделай таблицу" — ответ теперь `\| col \| col \|` вместо `<table>` |
| — | Запрос "переведи последнее сообщение в таблицу" при непустой истории диалога иногда путал модель: либо она искала "последнее сообщение" как термин в документном контексте ("В предоставленном контексте нет информации о последнем сообщении"), либо (при длинной истории, 5 обменов) сваливала несколько прошлых несвязанных реплик в одну таблицу вместо строго последней. Корень — `system_prompt_rag` требует "используй ИСКЛЮЧИТЕЛЬНО документный контекст", но такой запрос вообще не про документацию, а про переформатирование чужого текста из истории; `system_prompt_chat`, который должен был бы это разрулить, недостижим (см. ниже) | Новое правило 2б в `system_prompt_rag` — явно выделяет случай "переформатируй/сократи/перескажи конкретное прошлое сообщение" как отдельный, источник строго одна реплика (по умолчанию последняя), документный контекст для такого запроса игнорируется, смешивать несколько реплик запрещено. Воспроизведено и проверено двумя живыми прогонами (короткая история 3 обмена + длинная история 5 обменов, разные несвязанные темы) — оба теперь корректно берут только последнюю реплику |

---

## ✅ Исправлено (2026-08-05, route от plan + тул get_appendix)

Продолжение того же дня — по итогам изучения `AGENT_GRAPH_CURRENT.md` пользователь запросил
две конкретные правки: чтобы planner реально умел вызывать `get_appendix` как тул (не только
через обходной путь `reflect`/code-gate), и чтобы граф не шёл в базу знаний на запросы,
отвечаемые без поиска.

| # | Было | Как исправлено |
|---|---|---|
| — | `route` в `LeanAgentState` был захардкожен в `"domain_rag"` — `system_prompt_chat` не выбирался никогда, `decide_after_generate`'s smalltalk-ветка была недостижима | `plan_node` (`planning_nodes.py`) теперь дополнительно возвращает `route`: `"smalltalk"`, если `plan.subtasks` пуст (planner уже решает это по правилу 3 промпта), иначе `"domain_rag"`. Не воскрешает ML-роутер — использует уже существующее решение `plan` о том, нужен ли поиск. `decide_after_generate` (`routing_decisions.py`) упрощён — убран ранний выход по `route in {"smalltalk", "out_of_domain"}`, который стал бы регрессией: запрос-действие без похода в базу ("сохрани это в заметку") тоже получает `route="smalltalk"`, и раньше это молча пропустило бы проверку маркера действия. Теперь маркер проверяется всегда, независимо от route. Проверено живьём: "привет, как дела?" → `route: "smalltalk"`, поиск не запускался, мгновенный ответ; "сохрани это в заметку: купить хлеб" → `route: "smalltalk"`, `post_actions` всё равно сработал |
| — | `get_appendix` (текст приложений документа, см. A21 в `rag_service/ISSUES.md`) был реализован только как обходной путь — `reflect_node` (по LLM-сигналу `needs_appendix`) или regex-подстраховка в `build_prompt_node`, оба резолвят `doc_id` из уже найденного топового чанка. Если явный запрос пользователя ("найди приложение А в СП 1.13130") плохо матчился обычным поиском (мало содержательного текста для dense/BM25 — само приложение не индексируется), топовый чанк мог оказаться из **другого** документа с похожим номером, и приложение подтягивалось не то. Живой пример: "найди приложение А в СП 1.13130" вернуло приложение от "СП 2.13130.2020" | Новый метод `RetrievalService.find_document_id_by_code()` — ilike-поиск документа по названию/номеру через уже существующий `GET /documents?filename=` в rag_service. `get_appendix` зарегистрирован в `tool_registry.py` как настоящий Tool (`args: {document_code}`), внутри сам резолвит `document_code → doc_id → текст`. `plan_prompt` обновлён — planner теперь знает про оба тула и явно инструктирован звать `get_appendix`, когда пользователь называет документ и просит приложение, вместо `search_docs`. Результат идёт в `state.appendix_context` (новый `merge_appendix_result()` в `formatters.py`), не в `retrieval_data` — та же логика "отдельное поле, переживает повторный execute_subtasks", что и у reflect-пути. Проверено живьём: запрос с точным названием документа ("СП 1.13130", без выдуманного ".2020") корректно вернул приложение А именно этого документа, без похода в rerank/reflect вообще. ⚠️ Матчинг по-прежнему НЕ fuzzy — точный номер документа, отличающийся от реального названия в корпусе (лишний год, опечатка), не найдётся; для более гибкого поиска нужна отдельная работа |

---

## ✅ Исправлено (2026-08-05, фоновый аудит: старт-краш, JSON-контракт, обработка ошибок)

Третий проход того же дня — фоновый агент прогнал весь сервис на дыры/логику (файлы, не тронутые
предыдущими двумя проходами: `LLM_provider.py` целиком, `llm_gateway.py`, `infrastructure.py`,
`main.py`, `agent_routers.py`, `exceptions.py`) плюс отдельно копал баги графа за пределами уже
известного A5. A9/A10 (низкий приоритет, сегодня безвредны) залогированы выше как открытые,
остальное исправлено в этом же заходе.

| # | Было | Как исправлено |
|---|---|---|
| B4 | 🔴 `infrastructure.py::build_container()` безусловно вызывал `_build_query_router()` (`joblib.load` мёртвого ML-роутера — `route_node` отключён от графа, см. A5/legacy) без единого try/except, вызывалось из `main.py::lifespan` тоже без защиты. Любая проблема с артефактом (файл отсутствует/повреждён, несовместимая версия sklearn/joblib после апдейта) роняла старт **всего** сервиса — падали `/llm/answer`, `/llm/summary`, `/llm/note` и т.д. целиком, из-за компонента, который по факту никогда не вызывается | `_build_query_router()` — try/except, при сбое `logger.warning(..., exc_info=True)` + `return None` вместо падения. `query_router=None` безопасен - ничего живое `.route()` не вызывает. Тесты: `test_infrastructure.py` (новый файл) — сбой `joblib.load` не мешает получить `None`, нормальная сборка по-прежнему работает |
| B5 | `rerank_node`'s `rerank_query` брал `state.plan.subtasks[0].args.get("query", state.query)` вслепую — с тех пор как `get_appendix` стал вторым тулом (`args: {document_code}`, без ключа `"query"`), план вида `[get_appendix, search_docs]` (порядок не гарантирован промптом) тихо откатывал `rerank_query` на сырой `state.query`, воспроизводя тот же баг, что уже был найден и исправлен раньше (чанк получал 0.004 против сырого вопроса и 0.85 против переписанного) | Ищет первую подзадачу с ключом `"query"` в `args` (перебором `state.plan.subtasks`), а не берёт `subtasks[0]` вслепую — независимо от порядка `get_appendix`/`search_docs` в плане. Тест: `test_reranks_against_search_docs_query_when_get_appendix_is_first_subtask` |
| B6 | ⚠️ **Частично смягчено, корень не устранён.** `reflect_node`'s fast-path (`reflect_rounds >= 1`) полностью пропускает LLM-вызов — а значит и единственный штатный способ увидеть `needs_appendix` на втором круге. Сценарий: 1-й круг `need_more`, 2-й круг находит чанк, ссылающийся на приложение — сигнал никогда не выставляется, единственная страховка (regex-гейт в `build_prompt_node`) проверяла только `state.query`, не найденный на 2-м круге контент | Regex-гейт в `build_prompt_node` расширен — проверяет и `state.query`, И уже собранный `context_str` (найденные фрагменты), не только исходный вопрос. Сам fast-path `reflect_node` по-прежнему не зовёт LLM на 2-м круге (осознанный компромисс — задумывался, чтобы не зацикливаться на лишний LLM-вызов) — если ни вопрос, ни найденный текст не содержат подстроку "приложени", сигнал всё ещё может быть пропущен |
| B7 | 🔴 `LLMGateway.generate_json` (обслуживает `plan_node`/`reflect_node` — JSON-контрактные ноды, 100% живого трафика графа) делегировал в `LLM_provider.py::generate_general()`, которая **всегда** ставит системным сообщением `general_system_prompt` ("Ты — полезный инженерный ассистент. Отвечай кратко...") — ни слова про JSON. Инструкция "верни строго валидный JSON" жила только в user-сообщении (`plan_prompt`/`reflect_prompt`), конфликтуя с system-промптом обычного чата — повышало шанс, что модель ответит текстом/дисклеймером вместо чистого JSON сверх того, что уже лечит один ретрай в `generate_json` | Новый метод `generate_json_raw()` (оба провайдера + `LLMProvider` Protocol) — отдельный system-промпт `json_contract_system_prompt` (новый в `ai_config.toml`, только про JSON-контракт), не конфликтует с user-инструкциями. `generate_general()` не тронут — им по-прежнему пользуется легаси `expand_queries_node`. Тесты обновлены (`make_agent()`/`test_llm_gateway.py` теперь мокают `generate_json_raw`) |
| A6 | 🟠 Типизированная иерархия исключений (`exceptions.py`: `RagUnavailableError`/`RagResponseError`/`RerankerUnavailableError`/`RerankerResponseError`, с разными status_code 502/503) и её глобальный `@app.exception_handler(LLMServiceError)` в `main.py` были недостижимым кодом на живом трафике — оба реальных источника этих исключений (`retrieval_service.py`/`reranker_service.py`) вызываются только внутри `agent.run()`, а `/llm/answer` оборачивал этот вызов в собственный широкий `except Exception`, перехватывающий их раньше, чем они могли бы дойти до глобального хендлера. Backend всегда получал `502 "LLM pipeline failed: ..."` независимо от природы сбоя (временная недоступность vs ответ с ошибкой), хотя код для различения статусов существовал и производил впечатление рабочего | `agent_routers.py::answer_question` — добавлен `except LLMServiceError: raise` перед общим `except Exception`, пробрасывает типизированные исключения дальше нетронутыми, глобальный хендлер их теперь реально ловит. `/llm/answer/stream` не тронут — там `except Exception` осознанно ловит всё (SSE, `HTTPException` после отправки заголовков невозможен, см. докстринг роута) |
| A7 | 🟠 `no_data_node` не трогал `retrieval_data` — `reflect_node` может выставить `not_in_corpus` при непустом (просто слабом/нерелевантном) `retrieval_data`, и оба входа (`/llm/answer` пересчитывает `sources` из `final_state["retrieval_data"]`, `/llm/answer/stream` явно зовёт `extract_sources_node` перед выдачей) в итоге отдавали пользователю "В базе знаний нет информации по вашему запросу" **вместе со списком источников** — выглядит как противоречие | `no_data_node` теперь дополнительно возвращает `"retrieval_data": []`. Тест: `test_clears_retrieval_data`. Проверено живьём: запрос, форсирующий `no_data`, → `sources: []`, `retrieval_empty: true`, без противоречия |
| A8 | 🟠 `AsyncOpenAI`-клиент LLM-провайдера (`OpenAICompatLLMProvider`/`GroqLLMProvider`) не закрывался при остановке сервиса — `main.py::lifespan` явно закрывал `retrieval_service`/`reranker_service` (тот же паттерн синглтон-клиента из истории проекта), но про LLM-клиент забыли; метода `close`/`aclose` у провайдеров не было вообще | Добавлен `aclose()` в оба провайдера (`await self._client.close()`) + в `LLMProvider` Protocol, вызывается в `main.py::lifespan` симметрично retrieval/reranker |

**Побочная находка при живой проверке (не из аудита, не исправлено):** блок `[Приложения документа]`, который `build_prompt_node` дописывает в контекст, не содержит названия/кода документа, которому принадлежит текст — только сам текст приложения. Живой пример: на запрос "найди приложение А в СП 1.13130" модель получила правильный текст, но ответила "в контексте нет упоминания СП 1.13130... однако в представленном фрагменте присутствует Приложение А" — не смогла подтвердить из самого блока, что это приложение именно нужного документа, хотя это так. Дешёвый фикс — включить `document_code`/`source` в текст блока (`generation_nodes.py::build_prompt_node`, оба места, где собирается `appendix_context`).
