# ASGI / FastAPI
# Версии синхронизированы с llm_service (уже проверенная связка) - нужен fastapi>=0.136
# ради нативного fastapi.sse (EventSourceResponse/ServerSentEvent), это тянет за собой
# мажорный скачок starlette 0.x -> 1.0
fastapi==0.136.1
starlette==1.0.0
uvicorn==0.46.0
fastapi-limiter
python-multipart==0.0.20

# Database
sqlalchemy==2.0.46
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.18.4

# Auth - JWT + password hashing (pwdlib uses argon2)
pyjwt==2.11.0
pwdlib==0.3.0
cryptography==46.0.5
argon2-cffi==23.1.0
argon2-cffi-bindings==25.1.0

# Validation / Settings
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
annotated-types==0.7.0

# HTTP client (calls to llm_service / rag_service)
httpx==0.28.1

# Env / Config
python-dotenv==1.2.1

# Logging
python-json-logger==4.1.0

# Monitoring
prometheus-client

# UUID7 (used in Chats and Messages models)
uuid6==2024.7.10

# WebSockets
websockets==15.0.1

# Async runtime
anyio==4.13.0
greenlet==3.3.1

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
testcontainers==4.15.0

# Misc transitive deps
orjson==3.11.7
typing_extensions==4.15.0