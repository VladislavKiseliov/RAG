# Мессенджер — живой остаток

Бэкенд полностью реализован (WS, чаты, участники, поиск пользователей). Итоговый план фичи и
фронтенд-хвосты, которые ещё не сделаны, — в `TODO.md` (Фича 3, Фаза 2 приоритетного плана).
Этот файл — только то, чего нет в `TODO.md`: ручные шаги проверки.

## Проверка

```
Postman: POST /messenger/chats/direct, GET /messenger/chats/
Postman: GET /api/users/search?q=иван
Браузер: два окна — один пишет, второй получает по WS
```